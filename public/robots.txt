User-agent: *
Allow: /

# The site is intentionally kept out of search results via
# <meta name="robots" content="noindex, nofollow"> and the
# X-Robots-Tag response header. Crawling stays allowed so search
# engines can see the noindex directive and drop existing pages.
